service: s3-file-uploader

provider:
  name: aws
  runtime: nodejs18.x
  stage: dev2
  region: ap-south-1

custom:
  fileUploadBucketName: ${self:service}-bucket-${self:provider.stage}-v1

resources:
  Resources:
    FileBucket:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: ${self:custom.fileUploadBucketName}
        AccessControl: PublicRead
